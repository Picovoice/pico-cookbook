import { getStreamSampleRate, init, start, release } from './main';
import { AudioStream } from './audio_stream';

export default {
  init,
  start,
  release,
  getStreamSampleRate,
  AudioStream,
};
